name: Pull request checks 🔎

on:
  pull_request:

jobs:
  lint:
    name: Check code quality 🫆
    runs-on: ubuntu-latest
    steps:
      - name: Check code quality 🫆
        uses: tchoupinax/repo-config/shared-actions/golang/goreleaser-build-only@db89e9d50f7a2f66da683a659213997894ff2155

  build:
    name: Check build success 🦫
    runs-on: ubuntu-latest
    steps:
      - name: Check build success 🦫
        uses: tchoupinax/repo-config/shared-actions/golang/goreleaser-build-only@db89e9d50f7a2f66da683a659213997894ff2155

  labels:
    name: Autodetect labels 🔍 
    runs-on: ubuntu-latest
    steps:
      - name: Autodetect labels 🔍 
        uses: tchoupinax/repo-config/shared-actions/dx/automatically-apply-labels@db89e9d50f7a2f66da683a659213997894ff2155
        with:
          appId: ${{ secrets.DEPLOY_APP_ID }}
          privateKey: ${{ secrets.DEPLOY_APP_KEY }}
